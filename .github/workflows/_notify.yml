# Generated file: !!! DO NOT EDIT !!!
---
env:
  PYPERFORMANCE_HASH: f451bb0721d535b180ac1dc2ffe5cf02e0806120
  PYSTON_BENCHMARKS_HASH: 004743ccbd9e54598c543d7eb71fd3b8e10d5750
name: _notify
on:
  workflow_call:
    inputs:
      fork:
        type: string
      ref:
        type: string
      head:
        type: string
      date:
        type: string
      version:
        type: string
      tier2:
        type: boolean
      jit:
        type: boolean
      nogil:
        type: boolean

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
    - uses: technote-space/workflow-conclusion-action@v3
    - name: Checkout benchmarking
      uses: actions/checkout@v4
    - name: Setup system Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: pip
    - name: Install dependencies from PyPI
      run: |
        pip install -r requirements.txt
    - name: Send notifications
      if: env.WORKFLOW_CONCLUSION == 'success'
      run: python -m bench_runner notify --fork ${{ inputs.fork }} --ref ${{ inputs.ref
        }} --head ${{ inputs.head }} --date ${{ inputs.date }} --version ${{ inputs.version
        }} --tier2 ${{ inputs.tier2 }} --jit ${{ inputs.jit }} --nogil ${{ inputs.nogil
        }}
      env:
        GH_TOKEN: ${{ github.token }}
